
---

# 📻 StationsWithoutRDS — инструкция по настройке

Плагин **StationsWithoutRDS** помогает определить, **какая станция вещает на частоте**, если **RDS отсутствует**.
Он сравнивает текущую частоту с базой станций и показывает **самый вероятный вариант**.

---

## 📁 Где находится конфигурация

Файл конфигурации:

```
plugins_configs/StationsWithoutRDS.json
```

Если файла нет — он создаётся автоматически при запуске сервера.

---

## 🧩 Полный пример конфигурации

```json
{
  "mode": 3,
  "myStantions": [
    {
      "antenna": 0,
      "freq": 104.7,
      "station": "Radio Rossii",
      "location": "Biysk",
      "itu": "RUS",
      "distance": 13,
      "azimuth": 19,
      "pol": "V",
      "erp": 8
    }
  ],
  "thresholdSignal": 10,
  "stableTime": 3,
  "maxDistanceKm": 500,
  "refreshStationsMs": 24
}
```

---

# ⚙️ Описание параметров

## 🔁 `mode` — режим работы плагина

Определяет, **откуда берётся список станций**.

### Значения:

### `1` — только база **maps.fmdx.org** (по координатам QTH)

* Автоматически загружает станции с сайта maps.fmdx.org
* Ничего вручную заполнять не нужно
* Подходит большинству пользователей

```json
"mode": 1
```

---

### `2` — только **свои станции** (`myStantions`)

* Используется **только ваш список**
* Полезно, если вы:

  * знаете все местные передатчики
  * хотите исключить «далёкие» варианты

```json
"mode": 2
```

---

### `3` — **комбинированный режим** (рекомендуется ✅)

1. Сначала проверяет `myStantions`
2. Если не найдено — ищет в maps.fmdx.org

```json
"mode": 3
```

➡ **Рекомендуется для точного DX-приёма**

---

## 📻 `myStantions` — список своих станций

Позволяет вручную добавить станции, которые:

* точно принимаются у вас
* могут отсутствовать в базе maps.fmdx
* имеют нестандартные параметры

### Пример одной станции

```json
{
  "antenna": 0,
  "freq": 104.7,
  "station": "Radio Rossii",
  "location": "Biysk",
  "itu": "RUS",
  "distance": 13,
  "azimuth": 19,
  "pol": "V",
  "erp": 8
}
```

### Описание полей

| Поле       | Что это                                                |
| ---------- | ------------------------------------------------------ |
| `antenna`  | Номер антенны (не обязательный параметр)               |
| `freq`     | Частота в МГц                                          |
| `station`  | Название станции                                       |
| `location` | Город / населённый пункт                               |
| `itu`      | Код страны (RUS, KAZ, DEU и т.д.)                      |
| `distance` | Расстояние от вас в км                                 |
| `azimuth`  | Направление на передатчик (в градусах)                 |
| `pol`      | Поляризация (`V` — вертикальная, `H` — горизонтальная) |
| `erp`      | Мощность передатчика (кВт)                             |

📌 **Совет:**
Добавляйте сюда **ближайшие и самые важные станции** — они будут показываться первыми.

---

## 📶 `thresholdSignal` — минимальный уровень сигнала

```json
"thresholdSignal": 10
```

Минимальный уровень сигнала (в **dBµV**), при котором плагин начинает анализ частоты.

* Меньше → реагирует чаще
* Больше → игнорирует слабые сигналы

🔧 Рекомендуемые значения:

* Город: `12–15`
* Пригород / DX: `8–10`

## 📶 `thresholdSignals` — минимальные уровни сигнала (приоритетнее чем thresholdSignal)

```json
"thresholdSignals": {
  "all": 10,
  "95.0": -5,
  "97.0-108.0": 15
}
```

### Описание полей

| Поле         | Что это                                                |
| ----------   | ------------------------------------------------------ |
| `all`        | Все частоты (не обязательный параметр)                 |
| `95.0`       | Конкретная частота                                     |
| `97.0-108.0` | Диапазон частот                                        |
---

## ⏱ `stableTime` — время стабилизации частоты

```json
"stableTime": 3
```

Сколько секунд частота должна быть **неизменной**, прежде чем плагин начнёт поиск станции.

* Защищает от ложных срабатываний при прокрутке диапазона
* Значение указывается **в секундах**

🔧 Рекомендуется: `3`

---

## 🌍 `maxDistanceKm` — максимальная дистанция поиска

```json
"maxDistanceKm": 500
```

Максимальное расстояние (в км), на котором плагин ищет станции.

* Ограничивает дальние варианты
* Ускоряет работу

🔧 Рекомендуется:

* Город / регион: `300–500`
* DX: `1000+`

---

## 🔄 `refreshStationsMs` — обновление базы maps.fmdx

```json
"refreshStationsMs": 24
```

Как часто сервер **обновляет список станций** с maps.fmdx.org.

⏱ Значение указывается **в часах**.

* `24` — раз в сутки (рекомендуется)
* `12` — два раза в сутки
* `6` — часто (обычно не нужно)

---

# 🖼 Логотипы станций

Плагин автоматически ищет логотип:

1. В папке:

```
web/logos/
```

Пример:

```
web/logos/RADIOROSSII.png
web/logos/AVTORADIO.svg
```

2. Если локального логотипа нет — берёт с **tef.noobish.eu**
3. Если ничего не найдено — используется стандартный логотип

📌 Название файла должно быть похоже на название станции
(пробелы и регистр не важны)

---

# ✅ Рекомендованная настройка (для большинства)

```json
{
  "mode": 3,
  "myStantions": [],
  "thresholdSignal": 10,
  "stableTime": 3,
  "maxDistanceKm": 500,
  "refreshStationsMs": 24
}
```

---
